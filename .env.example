# Network Configuration
# Configure private keys for your accounts here
# NEVER commit this file with real private keys - use .env instead

# Account Private Keys (comma-separated, no 0x prefix)
# Example: VITE_PRIVATE_KEYS=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80,59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d
VITE_PRIVATE_KEYS=

# Optional: Default Network ID
# If not set, will use the first network defined in networks.config.json
# VITE_DEFAULT_NETWORK=besu-local

# Blockchain Indexer API URL
# The indexer provides efficient querying of blocks, transactions, and events
# Default: http://localhost:3001 (local development)
# Production: Indexer is behind nginx on the same port as Besu RPC
# Example: VITE_INDEXER_API_URL=http://130.61.22.253:8545
# VITE_INDEXER_API_URL=http://localhost:3001

# Vercel Deployment Configuration
# Only needed when deploying to Vercel with RPC proxy

# RPC Proxy Target (for /api/rpc endpoint)
# Point this to your remote Besu node's RPC endpoint
# Example: RPC_PROXY_TARGET=http://your-besu-node.example.com:8545
# RPC_PROXY_TARGET=http://152.70.182.220:8545
